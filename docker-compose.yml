version: "2"
services:
  web:
    container_name: app
    restart: always
    build: .
    ports:
      - "3000:3000"
    links:
      - db
    command: npm start
  db:
    container_name: mongo
    image: mongo
    volumes:
      - ./data:/data/db
    ports:
      - "27017:27017"